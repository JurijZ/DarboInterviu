version: 2
jobs:
  webapi:
    working_directory: ~/WebApi
    docker:
      - image: microsoft/dotnet:latest
    steps:
      - checkout
      - run: 
            name: Restore dependencies
            command: dotnet restore
      - run: 
            name: Build the projectdotnet
            command: dotnet build
